unit Unit1;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, ExtCtrls, sPanel, sDBNavigator, sSplitter, Grids, DBGrids,
  acDBGrid, StdCtrls, sComboBox, sEdit, sButton, sLabel, ComCtrls,
  sPageControl, ComObj, acTitleBar, sScrollBox, sFrameBar, acSlider,
  acAlphaHints, jpeg, acPNG, acImage, sSkinProvider;

type
  TForm1 = class(TForm)
    sSplitter1: TsSplitter;
    sButton1: TsButton;
    sButton2: TsButton;
    sImage1: TsImage;
    sLabel1: TsLabel;
    sSkinProvider1: TsSkinProvider;
    procedure sButton2Click(Sender: TObject);
    procedure sButton1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form1: TForm1;
  kg,kp:integer;
  p:string;
  mass: array of string;

implementation

uses Unit2, Unit3, Unit4, Unit5;

{$R *.dfm}


///////////////////////////////////////////////////////////////

procedure TForm1.sButton2Click(Sender: TObject);
begin
form3.Show;
end;

procedure TForm1.sButton1Click(Sender: TObject);
begin
form4.Show;
end;

end.
